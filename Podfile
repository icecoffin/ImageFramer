platform :ios, '12.0'

inhibit_all_warnings!

target 'ImageFramer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageFramer
  pod 'JGProgressHUD' 
  pod 'SnapKit'
  pod 'SwiftLint'

  target 'ImageFramerTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'
  end

end
